---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: observatory-yotalent
  namespace: security-audit
  labels:
    app: security-audit
    tool: observatory
    target: yotalent
spec:
  schedule: "0 6 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 300
      template:
        spec:
          restartPolicy: OnFailure
          serviceAccountName: audit-scanner
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: observatory-scanner
              image: ghcr.io/YOUR_ORG/audit-observatory:YOUR_TAG
              imagePullPolicy: Always
              env:
                - name: TARGET_URL
                  value: "https://yotalent.example.com"
                - name: TARGET_NAME
                  value: "yotalent"
                - name: COLLECTOR_URL
                  value: "http://collector:8080"
              resources:
                requests:
                  cpu: "50m"
                  memory: "64Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop: ["ALL"]

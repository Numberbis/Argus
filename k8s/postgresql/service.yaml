---
apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: security-audit
  labels:
    app: postgresql
spec:
  selector:
    app: postgresql
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP

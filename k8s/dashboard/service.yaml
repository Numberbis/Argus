---
apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: security-audit
  labels:
    app: dashboard
spec:
  selector:
    app: dashboard
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  type: ClusterIP

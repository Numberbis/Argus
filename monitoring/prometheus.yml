global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: "audit-collector"
    static_configs:
      - targets: ["collector:8080"]
    metrics_path: /metrics

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

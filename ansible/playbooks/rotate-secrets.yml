---
- name: Rotation des secrets
  hosts: localhost
  gather_facts: false
  roles:
    - role: secrets
  post_tasks:
    - name: Redémarrage du collector (rechargement des env vars)
      command: kubectl rollout restart deployment/collector -n {{ k8s_namespace }}
